spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dadoc?useSSL=false$serverTimeZone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect


  h2:
    console:
      enabled: TRUE
      path: /h2-console